<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.carpedm.mainpages">

	<select id="selectBook" resultType="BookDTO">
		select * from book
	</select>
	
	<select id="selectLibList" resultType="LibraryDTO">
		select lb_name,lb_opentime,lb_address,lb_tel,lb_content from library
	</select>
	
</mapper>